# 版本更新原则

> 本项目（健身自律记录工具）的发布流程遵循以下原则，发布时应严格照此执行。

## 核心原则

**每次推送到远程仓库，都应视为一次版本更新，并同步更新版本号，以便应用能检查到更新。**

具体而言，每次对外发布 / 推送远程，必须同时完成以下四件事，缺一不可：

1. **更新版本号**
   在 `健身打卡App/index.html` 顶部修改 `APP_VERSION` 常量，按递增规则升一个版本
   （如 `1.0.0 → 1.1.0 → 1.2.0`）。版本号是应用检测更新的依据，不递增则已安装用户收不到新版。

2. **重建 APK**
   将改好的 `健身打卡App/index.html` 同步到安卓工程并重新构建：
   ```bash
   cp 健身打卡App/index.html 健身打卡App-desktop/web/index.html
   cd 健身打卡App-desktop && npx cap sync android && ./android/gradlew.bat assembleDebug -p ./android
   cp android/app/build/outputs/apk/debug/app-debug.apk ../健身打卡App.apk
   ```

3. **发布带对应 tag 的 Release 并上传新 APK**
   用新的 `APP_VERSION` 创建 tag（如 `v1.1.0`），把新构建的 APK 作为资产上传，
   这样应用内置的“在线更新”才能通过 GitHub Releases 检测到并提示用户下载。

4. **推送代码到远程仓库**
   在完成上述动作后，再将该版本对应代码（`index.html`、`README`、说明文档等）推送到远程。

## 为什么不更新的后果

- 只改代码不升版本号：已安装用户打开 App 时，启动检测到最新 Release 仍是旧版本号，
  会判定“已是最新版本”，改动无法触达用户。
- 升级版本号但不发布 Release：应用查询不到对应 tag，同样检测不到更新。

## 快速发布检查清单

- [ ] `APP_VERSION` 已递增
- [ ] APK 已重新构建
- [ ] Release `v<新版本>` 已创建并上传新 APK
- [ ] 代码已推送远程
- [ ] README 中直接下载地址的版本号已同步（如有）